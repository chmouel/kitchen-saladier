============
Installation
============

At the command line::

    $ pip install kitchen-saladier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kitchen-saladier
    $ pip install kitchen-saladier